services:
    api:
        build: .
        ports:
            - "3333:3333"
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        env_file:
            - .env
        command: npm run dev
